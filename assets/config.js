/* =========================================================
   assets/js/config.js
   Phase 1 Step 1 — empty placeholder.

   Future purpose (Phase 2+):
   This file will hold static, non-secret configuration values
   currently hardcoded inline in index.html — for example the
   Supabase project URL/anon key, Tailwind theme tokens, and any
   other environment-level constants.

   No variables have been moved here yet. index.html is still
   the single source of truth for all configuration values at
   the end of Phase 1 Step 1.
========================================================= */
